# Phase 2 — Session 3: Bode 선도, 안정 여유, root locus

> 목표: "안정한가?"를 **pole을 풀지 않고** 판정하는 법. 특성방정식 `1 + L(s) = 0`을
> 근을 구해서 보는 대신 **`L(jω)`가 −1에 얼마나 가까운지**로 읽는다.
> 그러면 지연·필터·게인이 안정성을 얼마나 깎는지가 **각도(degree) 단위로 측정 가능**해진다.
> 선행: phase2-pid-problems.md (Session 2), phase3-fir-problems.md (주파수응답)

## 갚아야 할 빚 세 개

이 세션은 지난 세션들에서 이름 없이 지나간 것들을 회수한다.

1. **B2 (Session 1)**: "왜 Y가 유한하다고 가정해? 발산할 수도 있잖아" → `1+GH=0`이 그 경계였다.
   이제 그게 언제 일어나는지 잰다.
2. **P4 (Session 2)**: "D의 노이즈를 필터로 잡으면 지연이 생기고, 지연은 제어에 독" →
   그 독의 양을 숫자로 만든다.
3. **W5 (Session 2)**: "K를 키워도 pole 실수부가 −1에 갇힘" → 그 궤적의 이름이 root locus.

## 도구 정리

### Bode 선도 = 주파수응답을 로그로 두 장

`s = jω`를 대입한 `L(jω)`는 복소수다. 복소수는 **크기와 각도** 두 개니까 그래프도 두 장:

```
① magnitude:  20·log₁₀|L(jω)|  [dB]   vs  log ω
② phase:      ∠L(jω)  [degree]        vs  log ω
```

**왜 로그인가**: 전달함수는 곱으로 쌓인다(`L = C·G·H`). 로그를 씌우면 **곱이 합**이 된다.

```
|L| = |C|·|G|·|H|        →   dB로:  20log|L| = 20log|C| + 20log|G| + 20log|H|
∠L = ∠C + ∠G + ∠H       →   각도는 원래 합
```

부품별 그래프를 **더하기만 하면** 전체가 나온다. 손으로 그릴 수 있는 이유.

### 부품별 점근선 (이 4개면 대부분 그린다)

| 부품 | magnitude | phase |
|---|---|---|
| 상수 `K` | `20log K` [dB], 평평 | `0°` |
| 적분기 `1/s` | `−20 dB/decade`, ω=1에서 0dB | `−90°` (모든 ω) |
| 미분기 `s` | `+20 dB/decade` | `+90°` |
| 1차 pole `1/(s+a)` | ω≪a: 평평(`−20log a`)<br>ω≫a: `−20 dB/decade` | 0° → −90°, **ω=a에서 −45°** |

- **corner frequency (꺾이는 지점) = pole의 크기 `a`**. 그 전엔 평평, 그 후엔 기울어진다.
- **pole 하나 = −20dB/dec + 최대 −90°**. zero 하나 = +20dB/dec + 최대 +90°.
- decade = 10배. `−20dB/dec` = 주파수 10배마다 이득 1/10.

> Phase 3의 "거리 방법"(`|H| = ∏zero거리/∏pole거리`)과 같은 것을 로그 축에서 본 것.
> 그때는 단위원 위를 돌았고, 여기서는 허수축을 따라 올라간다.

### 왜 하필 −1인가 ★ 이 세션의 심장

특성방정식이 `1 + L(s) = 0`, 즉 **`L(s) = −1`** 이다.
어떤 주파수 ω에서 `L(jω) = −1`이면 그 ω가 **허수축 위의 pole** — 죽지도 발산하지도 않는 영구 진동.

`−1`이라는 복소수를 크기·각도로 읽으면:

```
|L(jω)| = 1        (크기 1: 루프를 한 바퀴 돌아도 신호가 안 줄어듦)
∠L(jω) = −180°     (위상 180° 뒤집힘: 빼주려던 게 더해줌 = feedback이 positive가 됨)
```

**두 조건이 같은 ω에서 동시에 만족되면 발진한다.** 하나라도 어긋나면 살아남는다.
그래서 "얼마나 여유가 있나"를 두 방향으로 잰다:

```
gain margin (GM)   : ∠L = −180°인 주파수에서, |L|이 1까지 가려면 몇 배 남았나
phase margin (PM)  : |L| = 1인 주파수(ω_gc, gain crossover)에서, ∠L이 −180°까지 몇 도 남았나
```

```
PM = 180° + ∠L(jω_gc)          단, |L(jω_gc)| = 1
GM = 1/|L(jω_pc)|              단, ∠L(jω_pc) = −180°
```

**실무 목표: PM 45~60°, GM 6dB(2배) 이상.** PM 30° 미만이면 진동이 심하고 위험.

경험 규칙: **`ζ ≈ PM/100`** (PM 60° → ζ≈0.6, overshoot 10% 남짓). Session 2의 ζ와 바로 연결된다.

### 지연은 위상만 깎는다 ★ P4의 정량화

순수 지연 `T`초의 전달함수는 `e^(−sT)`. 여기에 `s=jω`를 넣으면:

```
|e^(−jωT)| = 1                    ← 크기는 전혀 안 변함
∠e^(−jωT) = −ωT [rad] = −57.3·ωT [°]   ← 위상만, ω에 비례해서 무한히
```

**크기 그래프에는 아무 흔적도 안 남기고 위상만 갉아먹는다.** 그래서 시간 응답만 보면 "조금 늦네"인데
안정성은 소리 없이 무너진다. 그리고 ω에 **비례**하므로 고주파에서 치명적이다.

여기서 실무 공식 하나가 나온다:

```
견딜 수 있는 최대 지연:  T_max = PM[rad] / ω_gc
```

**필터를 넣기 전에 이 숫자부터 계산한다.** 드론 프로젝트의 딜레마가 이 한 줄로 정리된다.

### Root locus = K가 변할 때 pole이 그리는 궤적

`1 + K·G(s) = 0`에서 K를 0 → ∞로 쓸어가며 근을 찍은 그림. Session 2에서 손으로 이미 했다.

- **K=0**: 근 = `G`의 pole (open-loop pole). **궤적의 출발점**
- **K→∞**: 근 = `G`의 zero로 가거나, zero가 모자라면 **무한대로 발산**
- 실수축 위: 오른쪽의 pole+zero 개수가 **홀수**인 구간이 궤적에 포함
- 무한대로 가는 가지들은 **점근선**을 따라간다: 각도 `(2k+1)·180°/(n−m)`, n=pole수, m=zero수

**궤적이 허수축을 넘어가는 순간의 K가 안정 한계**다. Bode의 gain margin과 같은 정보를 다른 그림으로 본 것.

> 표기: ✍️ = 네가 쓴 답 그대로 / 💬 = Claude가 덧붙인 해설·교정

---

## 문제

### ✅ D1. −1의 의미 — 전 문항 자력, 파생 질문이 세션의 하이라이트 ★

- ✍️ (a) "진동이 계속 남아" — 허수축 pole = 영구 진동, 자력
- ✍️ (b) **"+LE"** — `E = R − L·E`에서 L이 부호를 뒤집으면 `E = R + |L|·E`,
  **negative feedback이 positive feedback으로** 바뀜을 자력 도달.
  💬 보강: `E = R/(1−|L|)`, `|L|=1`이면 분모 0 → 유한한 R로 무한한 E (하울링)
- ✍️ (c) "작아져 절반씩" — `0.5ⁿ → 0`. 부호가 계속 자기를 밀어주는데도 크기가 못 버텨 죽음.
  💬 Phase 3 연결: **단위원 안 pole `|p|ⁿ→0`과 같은 구조** — 루프 한 바퀴당 이득이 그 역할

**세션 시작 전 개념 질문 (전부 사용자 발) — 이 세션의 실질적 내용이 여기서 나옴:**

- 💬 "특성방정식이 뭔지부터 보자" → 분모의 근이 응답의 **종류**를, 분자가 **섞이는 양**을 결정.
  Session 2 P5의 실패(pole 맞고 overshoot 틀림)가 이 구분을 안 지킨 것이었음
- 💬 **"왜 하필 jω를 대입해?"** → ① 허수축 = 안정 경계라 거기서 재는 게 자연스럽고,
  ② **허수축만 물리적으로 측정 가능**하다 (`L(jω)` = sine 넣었을 때 진폭비·위상차 = 실측 가능,
  `L(−5+2j)`에 해당하는 입력은 주입하지 않음). 그래서 Bode는 이론이면서 동시에 실측 데이터고,
  모델 없이도 그릴 수 있음(root locus·pole 계산은 모델 필수). Phase 1의 "X(jω)의 정체" 회수
- 💬 "ω는 여러 개 넣어보는 거야?" → 전 대역 sweep. 발진은 **같은 ω에서 두 조건이 동시에**
  만족될 때만 일어나므로 전부 확인해야 함. 실제 발진 시 나는 소리의 주파수가 그 ω
- ✍️ **"그럼 |L|이 1보다 작아야 하는 거지, −180° 근처에서?"** — gain margin의 정의를 자력 재발견
- ✍️ **"둘 중 하나만 피해도 되는 거 아니야?"** — 💬 **정확한 지적, 안정성만 놓고 보면 맞음**.
  둘 다 보는 건 안정성이 아니라 **여유** 때문 — GM은 이득 변동(부하·전압·개체차, B5의 50% 변동),
  PM은 위상 변동(계산 시간·통신 지연·필터·무시한 고주파 pole)에 대한 보험이고 서로를 대신 못 함.
  추가로 PM만이 응답 품질까지 정함(`ζ ≈ PM/100`)
- ✍️ **"둘을 같이 보는 유클리디안 거리로 재는 게 좋아?"** — 💬 **vector margin(modulus margin)에
  자력 도달**. `VM = min|1+L(jω)| = 1/max|S|`이고 `GM ≥ 1/(1−m)`, `PM ≥ 2·arcsin(m/2)`를 동시 보장.
  단 Bode는 두 장이라 거리가 안 보이고 **Nyquist(복소평면 한 곡선)** 라야 보임.
  GM/PM이 표준으로 남은 건 자로 읽히고 해석이 직관적이라서. **H∞ 제어가 정확히 `max|S|` 최소화를
  설계 목표로 삼는 분야** — 사용자 직감이 로버스트 제어가 실제로 간 방향과 일치. B5의 `S`가 재등장

### ✅ D2. Bode 그리기 — 손 점근선 (거의 자력, 로그 밑 혼동 1회 + GM 해석 1회 교정)

`L(s) = 8/(s(s+2))` (Session 2 워밍업의 plant에 K=8).

- 💬 "어떻게 해야 하는 거야?" → 레시피 제시: 부품마다 **① 기울기 ② 지나는 높이** 둘만 정하면 직선.
  로그를 씌우면 곱이 합이 되므로 부품별로 그려서 더함
- ✍️ **"분모 분자에 2−jω를 곱해야 하는 거 아니야?"** → 💬 유리화 불필요.
  `|1/z| = 1/|z|`, `∠(1/z) = −∠z`라 분모만 보면 됨. **유리화는 실수부/허수부를 따로 알아야 할 때
  (Nyquist 작도) 필요** — Bode를 손으로 그릴 수 있는 이유가 이 분해성
- ✍️ (a) 부품2 `1/s`: ω=1에서 0dB, ω=10에서 −20dB, 기울기 −20dB/dec — 자력
  부품3 `1/(s+2)`: 저주파 −6dB 평평, 고주파 −20dB/dec — 자력
  ⚠️ corner 계산에서 **`10^0.3` 대신 `e^0.3`을 써서 1.34** → 💬 밑 지적 → ✍️ **ω = 2** 교정
  💬 규칙: **corner frequency = |pole|** (`√(ω²+a²)`에서 지배항이 바뀌는 지점).
  Bode는 pole 위치만 보면 꺾이는 곳이 계산 없이 나옴
- ✍️ 합계: 저주파 **−20 dB/dec**, 고주파 **−40 dB/dec** — 자력
  💬 회수: **고주파 기울기 ÷ (−20) = pole 개수** → "내 모터가 몇 차인지" 질문의 실측 답
- ✍️ (b) ω→0: −90°, ω→∞: −180°, ω=2: **−135°** — 전부 자력
- ⚠️ (c) 첫 답 "gain margin이 0에 가까워져" (방향 반대) → 💬 `GM = 1/|L(ω_pc)|`이고
  `ω_pc`가 ω→∞에서만 나타나며 거기서 `|L|→0` → ✍️ **"무한대"** 교정
- 💬 landing: **GM = ∞** — K를 아무리 키워도 불안정 불가. **W5의 "실수부가 −1에 갇힘"과 같은 사실의
  다른 그림.** 이유는 위상 예산: **pole 하나당 최대 −90°**, 2개면 −180°가 상한이고
  거기 닿는 건 ω→∞뿐인데 그때 이득이 이미 0. **pole 2개짜리는 원리적으로 발진 불가** → D5로 이어짐

### 🔵 D3. Phase margin 계산 — (a)(b) 완료, **(c)부터 다음 세션**

같은 `L(s) = 8/(s(s+2))`.

- ✍️ (a) `ω⁴ + 4ω² − 64 = 0` 자력 유도 → 치환 후 `ω² = 6.25` → **ω_gc = 2.5** 자력
- ✍️ (b) **PM = 180 − 90 − 51.3 = 38.7°** 자력, `∠L = −141°` 자력
- 💬 "위상을 어떻게 구해?" → 크기와 같은 구조로 **부품별 각도의 합**:
  `∠L = ∠8 + ∠(1/jω) + ∠(1/(jω+2)) = 0° − 90° − atan(ω/2)`.
  근거는 `∠(a+jb) = atan(b/a)`와 `∠(1/z) = −∠z` 두 개뿐

- ✍️ (c) `ζ ≈ 0.387` vs W4 손계산 `1/√8 = 0.354` — 9% 차이, 자력
  💬 의미: 왼쪽은 **Bode에서 눈금 두 개 읽어** 나온 값, 오른쪽은 특성방정식을 세워 표준형 매칭한 값.
  **근을 한 번도 안 풀고 응답 품질을 알아냄** → 모터에 sine을 넣어 Bode를 실측하면 모델 없이 ζ를 안다.
  단서: 2차 지배 시스템 + ζ<0.6 구간에서만 잘 맞음(다행히 실무 설계 영역 PM 40~70°가 그 구간)

### ✅ D4. 지연이 먹는 여유 — P4의 청구서 ★★ 오늘 가장 값나가는 문제

- ✍️ (a) **T_max = 0.27초 (270 ms)** 자력 — 게인을 하나도 안 건드렸는데 이 지연만으로 발진
- ✍️ (b) **55탭** 자력 (100Hz, `(N−1)/2` 샘플 지연)
- ✍️ (c) 예산 계산 → **−6.3°**. ✍️ **"이미 PM이 38.7인데 45를 어떻게 유지해"** — 함정을 자력 간파.
  💬 이게 P4 딜레마의 정직한 답: **여유는 공짜로 안 생기고, 내놓을 수 있는 건 게인뿐**
- 💬 loop shaping 도입: **위상 곡선은 K와 무관한 고정 지형이고, K는 그 위 어디에 crossover를
  내려놓을지만 정한다.** pole placement(근의 위치를 찍음)와 대비되는 두 번째 설계 방식
- 💬 "ω_gc가 뭐야?" → `|L|=1`인 주파수 = **위험 조건의 절반이 이미 충족된 지점**이라 거기서 PM을 잰다.
  `ω < ω_gc`는 제어가 작동하는 대역, `ω > ω_gc`는 포기한 대역 → **ω_gc ≈ 대역폭 ≈ 응답 속도**
- ✍️ PM 45° 설계: `atan(ω_gc/2)=45°` → **ω_gc = 2** → **K = 5.66** (8에서 29% 감소, 대역폭 20% 손실)
- ✍️ PM 60° 설계(필터 예산 15° 확보): **ω_gc = 1.154, K = 2.66, T = 0.22초, 45탭** 전부 자력

**전체 거래**:

| | K = 8 | K = 2.67 |
|---|---|---|
| PM | 38.7° | 60° |
| ω_gc (대역폭) | 2.5 rad/s | 1.155 rad/s |
| 필터 예산 | **0탭** (이미 기준 미달) | **45탭** |

**게인을 3배 포기해서 45탭 필터를 샀다.** 노이즈 억제와 응답 속도는 같은 통화(= phase margin, 단위 도)로 거래된다.

- ⚠️ (d) 첫 답 "지연이 한 탭 밀리는 게 **GM**을 깎아" → 💬 **PM**으로 교정.
  지연은 `|e^(−jωT)|=1`이라 크기 곡선을 아예 안 건드림.
  (다만 간접적으로 GM에도 영향 — 지연 때문에 유한한 ω에서 −180°에 닿게 되어 **무한대였던 GM이 유한해짐**.
  D2의 "pole 2개면 발진 불가"가 지연이 끼면 깨진다)
- 💬 탭당 단가: `1.155 × 0.005s × 57.3 ≈ 0.33°/탭` (45탭 × 0.33 ≈ 15° ✓).
  단가가 ω_gc에 비례하므로 **빠른 시스템일수록 필터가 비싸다**
- 💬 Phase 3 회수: IIR이 FIR보다 나았던 이유를 제어 관점으로 재해석하면
  **"같은 지연 예산으로 더 많은 노이즈를 산다"** — 예산이 도 단위로 정해져 있으니 필터 효율 = 제어 성능

**D4 결론**: 지연은 `ω_gc × T` [rad]만큼 phase margin을 깎는다. 크기 곡선을 안 건드리므로
시간 응답만 봐서는 안 보이고, 손실량이 대역폭에 비례하므로 빠른 시스템일수록 같은 필터가 더 비싸다.

### ✅ D5. Pole을 하나 더 — 진짜 한계가 생긴다 ★ (전 문항 자력, 방향 1회 교정)

`G(s) = 1/(s(s+2)(s+10))`. `s+10`은 "빠르니까 무시하자"던 전기 pole 또는 센서 필터.

- ✍️ (a) `s³ + 12s² + 20s + K = 0` 자력
- ✍️ (b) `s=jω` 대입 후 실수부·허수부 분리 → **ω = √20 ≈ 4.47, K = 240** 자력
- ⚠️ (c) "pole 하나당 −90도" 자력이나, **"예산에 제한이 생겼어"로 방향이 반대** →
  💬 위상 예산은 **제한된 게 아니라 늘어난 것**이고 그게 문제:
  pole 2개면 −180°가 상한이라 못 닿지만, **3개면 −270°까지 갈 수 있어 유한한 ω에서 −180°를 통과**한다.
  통과 지점에서 `|L|<1`이어야 하므로 K에 상한이 생김.
  **pole 개수가 위험의 존재 여부를, K가 그 위험에 얼마나 가까이 가는지를 정한다**
- ✍️ (d) "늘수록 작아져" → 💬 정밀화: **pole ≤ 2면 상한 없음, ≥ 3이면 유한한 상한 발생**(이분법),
  그 뒤로는 연속적으로 나빠짐. 상한 크기는 **무시한 pole이 얼마나 빠른가**가 결정 —
  `1/(s(s+2)(s+1000))`이면 K < 2,004,000 (pole을 100배 멀리 → 상한 8000배).
  따라서 **"무시해도 되는 pole"이라는 판단은 절대적이지 않고 K에 따라 달라진다**

**"K를 키우면?"에 대한 답의 3단 갱신**:

```
W2  1차 plant :  K↑ → 빨라진다             (참, pole 1개일 때만)
W5  2차 plant :  K↑ → 안 빨라지고 진동만 는다  (실수부 고정)
D5  3차 plant :  K↑ → 어느 지점부터 발산한다   (K < 240)
```

Session 1 B3의 "센서만 좋으면 K를 무한히 키워도 되나?"에 "위상 지연이 정하는 상한이 있다"고
답했던 것 — **그 상한이 240이고, 손으로 구했다.**

💬 실무적으로 가장 무서운 형태: 1·2차 모델로 튜닝할 땐 K를 올릴수록 좋아지기만 하다가,
무시했던 pole이 대역 안에 들어오는 순간 갑자기 발진. **모델에 없는 pole이라 시뮬레이션에도 안 보인다.**

### ✅ D6. Root locus — W5의 일반화 (전 문항 자력)

- ✍️ (a) `K=1 → s=−1`(중근), `K=4 → −1±j√3`, `K=100 → −1±j√99` 자력.
  실수축의 0과 −2에서 출발 → K=1에서 −1에서 만남(breakaway) → 수직으로 갈라져 무한대.
  💬 `s = −1±√(1−K)`에서 K=1이 분기점인 건 제곱근 부호가 바뀌는 자리 — Phase 1의 ζ=1과 같은 구조.
  **궤적이 우반평면에 발을 못 들임 = GM=∞**, 세 도구가 같은 사실을 말함
- ✍️ (b) 점근선 각도 **60°, 180°, 300°** 자력. centroid = −4. ±60° 가지가 우반평면을 향해
  ±j4.47에서 허수축 통과(K=240), −10에서 출발한 가지는 180°로 도망
- ✍️ (c) **"각도가 작아지면 실수부가 더 커짐 / pole이 늘면 각도가 작아지고 더 빨리 우반평면으로 간다"**
  — 자력, 정확. 💬 첫 점근선 각도가 `180°/(n−m)`이라 pole이 늘수록 0°에 가까워짐

**두 도구가 같은 문턱을 가리킨다**:

| | Bode | Root locus |
|---|---|---|
| 안전 조건 | 위상이 −180°에 못 닿음 | 점근선이 90° 이상 |
| n−m ≤ 2 | 최대 위상 지연 −180° | 90°, 180° |
| n−m ≥ 3 | −180° 통과 가능 | 60° 이하 → 우반평면 |

**pole 하나 = 위상 −90° = 점근선 각도를 좁히는 것.** 같은 사실의 두 표기.

---

## ✅ Python 검증 — 손계산과 전부 일치 (2026-07-25)

`phase2_bode_check.py`:

```
=== D2/D3  L = 8/(s(s+2)) ===
손계산:  ω_gc = 2.5      PM = 38.7°     GM = ∞
실측:    ω_gc = 2.4992   PM = 38.67°    GM = ∞
         vector margin = 0.5601
         ζ ≈ PM/100 = 0.387   vs  1/√8 = 0.354

=== D4  지연 ===
T =   0 ms  →  ω_gc = 2.499   PM = 38.67°
T =  50 ms  →  ω_gc = 2.499   PM = 31.51°
T = 150 ms  →  ω_gc = 2.499   PM = 17.19°
T = 270 ms  →  ω_gc = 2.499   PM =  0.01°   ← 손계산 T_max와 정확히 일치

=== D5 ===
K = 200  →  max Re = −0.127   stable
K = 240  →  roots = −12, ±4.472j   ← 정확히 허수축 (√20)
K = 280  →  max Re = +0.118   UNSTABLE
```

**`ω_gc`가 네 경우 모두 2.499로 고정**인 것이 D4의 핵심을 그대로 보여준다 —
지연은 크기 곡선을 한 톨도 안 건드리고 PM만 38.67 → 0으로 녹인다.

Fig 1 (Bode + 손 점근선), Fig 2 (지연이 위상만 먹는 모습 + step 응답),
Fig 3 (Nyquist — vector margin이 보이는 유일한 그림), Fig 4 (root locus 2개 vs 3개).
