# Phase 4 Session 3 — 최소제곱 (정사영의 일반화)

> 목표: 해가 **없는** $A\mathbf{x} = \mathbf{b}$를 다루는 법. "가장 가깝다 = 오차가 수직이다"에서
> 출발해 정규방정식 $A^TA\hat{\mathbf{x}} = A^T\mathbf{b}$까지 손으로 세운다.
> 끝에서 이미 알던 정사영 $(\mathbf{u}^T\mathbf{b})\mathbf{u}$가 **열이 하나일 때의 특수한 경우**임을 확인.
> 선행: Session 1(열공간·rank·동치 사슬)
> 도착지: Phase 6 — 칼만 게인이 **가중** 최소제곱의 가중치다

Session 1에서는 $A\mathbf{x} = \mathbf{b}$에 **해가 있느냐**를 물었다. 이번엔 **해가 아예 없을 때**.
측정이 미지수보다 많으면 항상 이렇게 된다(센서를 100번 읽어 파라미터 2개를 맞추는 상황).

## ✅ L1. 해가 없는 경우

$$
A = \begin{bmatrix} 1 & 0 \\ 0 & 1 \\ 0 & 0 \end{bmatrix}, \qquad
\mathbf{x} = \begin{bmatrix} x_1 \\ x_2 \end{bmatrix}, \qquad
\mathbf{b} = \begin{bmatrix} 1 \\ 2 \\ 3 \end{bmatrix}
$$

Session 1의 규칙 그대로 $A\mathbf{x}$는 **열의 조합**:

$$
A\mathbf{x} = x_1\begin{bmatrix}1\\0\\0\end{bmatrix} + x_2\begin{bmatrix}0\\1\\0\end{bmatrix} = (x_1, x_2, 0)
$$

- ✍️ **"택도 없는데? $(x_1, x_2, 0)$"** 자력 — 셋째 성분이 무슨 짓을 해도 0이라
  $\mathbf{b}$의 3과 만날 수 없다

$A$의 **열공간**(column space)이 $z=0$ 평면인데 $\mathbf{b}$가 그 평면 **밖에** 떠 있는 상황.
해가 없으니 질문을 바꾼다:

> 정확히 맞추는 게 불가능하면, **가장 가까운 것**으로 만족하자.

## ✅ L2. 가장 가깝다 = 오차가 수직이다

- ✍️ 가장 가까운 점 $(1, 2, 0)$, 오차 벡터 $\mathbf{e} = (0,0,3)$ — **평면에 수직** 자력

수직이 우연이 아닌 이유(피타고라스): 평면 위 다른 점을 아무거나 잡으면

$$
\text{거리}^2 = (\text{평면 안에서 옆으로 간 거리})^2 + 3^2 \;\ge\; 3^2
$$

> **"가장 가깝다"와 "오차가 수직이다"는 같은 말이다.** 최소제곱의 전부가 이것이고,
> 나머지는 이 조건을 식으로 옮기는 작업이다.

## ✅ L3. 수직 조건 → 정규방정식

평면에 수직인지 확인하는 법: **평면을 만드는 벡터들(= $A$의 열) 각각과 내적이 0**.

- ✍️ $\mathbf{e}\cdot(1,0,0) = 0$, $\mathbf{e}\cdot(0,1,0) = 0$ 자력

**transpose**는 행과 열을 바꾼 것이라 $A^T$의 **행 = $A$의 열**:

$$
A = \begin{bmatrix} 1 & 0 \\ 0 & 1 \\ 0 & 0 \end{bmatrix}
\;\Rightarrow\;
A^T = \begin{bmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \end{bmatrix}
$$

그래서 위의 내적 두 개를 한 줄로 압축하면 $A^T\mathbf{e} = 0$ ("$\mathbf{e}$가 $A$의 모든 열과 수직").
$\mathbf{e} = \mathbf{b} - A\hat{\mathbf{x}}$를 넣으면:

$$
A^T(\mathbf{b} - A\hat{\mathbf{x}}) = 0
\;\Longrightarrow\;
\boxed{A^TA\,\hat{\mathbf{x}} = A^T\mathbf{b}}
\qquad \text{(normal equation)}
$$

해가 **없는** $A\mathbf{x}=\mathbf{b}$를, 해가 **있는** 식으로 바꾼 것.
$A$는 $3\times2$라 정사각형이 아니었는데 $A^TA$는 $2\times2$ 정사각형이 된다.

## ✅ L4. 공식 검산

- ✍️ $A^TA = I_{2\times2}$, $A^T\mathbf{b} = (1,2)$, 따라서 $\hat{\mathbf{x}} = (1,2)$ 자력
  — L2에서 눈으로 찍은 답과 일치

### 왜 "최소제곱"인가

최소화한 것은 $\mathbf{b}$까지의 **거리**인데, 식으로 쓰면

$$
\|\mathbf{b} - A\mathbf{x}\|^2 = e_1^2 + e_2^2 + e_3^2
$$

**오차의 제곱합**. "가장 가까운 점 찾기 = 오차 제곱합 최소화"라 이름이 그렇게 붙었다.

## ✅ 정사영은 특수한 경우였다

로드맵의 "이미 아는 정사영 $(\mathbf{u}^T\mathbf{b})\mathbf{u}$의 일반화"가 이것.
$A$가 **열 하나짜리**(길이 1인 벡터 $\mathbf{u}$)인 경우를 넣으면:

$$
A^TA = \mathbf{u}^T\mathbf{u} = 1, \qquad
A^T\mathbf{b} = \mathbf{u}^T\mathbf{b}, \qquad
\hat{x} = \mathbf{u}^T\mathbf{b}
$$

$$
\text{투영된 점} = A\hat{x} = \mathbf{u}(\mathbf{u}^T\mathbf{b}) = (\mathbf{u}^T\mathbf{b})\mathbf{u}
$$

> **정사영 = 열이 하나일 때의 최소제곱.** 열이 여러 개로 늘어나면 나누기 자리에 $(A^TA)^{-1}$이 들어올 뿐:

$$
\text{투영} = A(A^TA)^{-1}A^T\,\mathbf{b}
$$

## ✅ 언제 깨지나 — Session 1 회수

- ✍️ **"rank가 무너진 상태?"** 자력 정답

$A$의 열이 **선형종속**이면 $A^TA$도 같이 무너진다. 조건은 $\text{rank}\,A = (\text{열의 개수})$
= full column rank.

| $A$의 열 | 결과 |
|---|---|
| 독립 | 열공간이 제대로 펼쳐짐 → $\hat{\mathbf{x}}$가 유일 |
| 종속 | 열공간이 납작함 → 같은 투영점을 주는 $\mathbf{x}$가 무한히 많음 |

정밀하게: **투영점 자체는 여전히 유일하다**(평면 위 가장 가까운 점은 하나뿐).
무너지는 건 "그 점을 만드는 계수 $\hat{\mathbf{x}}$"가 하나로 안 정해진다는 쪽 —
Session 1의 $B$에서 서로 다른 입력이 같은 출력이던 상황의 재발.

실무에서 깨지는 전형: **측정이 서로 중복될 때**(같은 정보를 주는 센서 둘 → 열이 종속).
이때 쓰는 것이 **pseudo-inverse**이고, 그것을 제대로 정의하는 도구가 **SVD**.

## 📌 Phase 6 예고 — 칼만이 여기서 나온다

최소제곱은 모든 측정을 **동등하게** 취급한다. 센서마다 신뢰도가 다르면:

| | 최소화 대상 |
|---|---|
| 최소제곱 | $\sum e_i^2$ |
| **가중** 최소제곱 | $\sum w_i e_i^2$, 　$w_i = 1/\sigma_i^2$ |

정밀한 센서에 큰 가중치를 준다. **칼만 게인이 정확히 이 가중치** —
"지금까지의 예측"과 "새 측정" 중 어느 쪽이 덜 불확실한지의 비율.
칼만 필터는 그것을 **측정이 올 때마다 재귀적으로** 한다.
Phase 6은 여기에 베이즈(이미 강한 부분)를 붙이면 된다.

---

## 진행 기록

| 항목 | 상태 | 메모 |
|---|---|---|
| L1 해가 없는 경우 | ✅ 2026-07-28 | "택도 없는데" — 열공간 밖의 $\mathbf{b}$를 즉시 파악 |
| L2 수직 = 최단 | ✅ 2026-07-28 | 최근접점·오차벡터·수직 관계 자력 |
| L3 정규방정식 | ✅ 2026-07-28 | 내적 조건 자력, $A^T$로의 압축은 해설 |
| L4 검산 + 정사영 일반화 | ✅ 2026-07-28 | $\hat{\mathbf{x}} = (1,2)$가 눈으로 찍은 답과 일치 |
| full column rank 조건 | ✅ 2026-07-28 | "rank가 무너진 상태?" 자력 — Session 1 회수 |

**총평**: 전 문항 자력. Session 1의 **열공간** 언어가 그대로 도구로 쓰인 회차 —
"해가 있느냐"(Session 1) → "없으면 어떻게 하느냐"(Session 3)로 같은 그림이 확장됐다.
Session 2의 특성방정식도 Session 1을 도구로 썼으므로, Phase 4는 **Session 1이 계속 회수되는 구조**다.

**다음**: 양의 정부호 행렬, SVD(개념만), 그래디언트 = 최급상승 (짧게) → **Phase 5 상태공간**
