# Phase 2 — Session 2: PID control

> 목표: controller `C(s) = K_p + K_i/s + K_d·s`의 세 항이 **특성방정식의 어느 자리에 들어가는지**로
> 각자의 역할을 이해한다. P만(정상상태 오차 잔존) → +I(오차 제거, 대가는 오버슛) → +D(감쇠) 순서.
> 설계 = 원하는 pole 위치를 찍고 게인을 역산하는 것(pole placement).
> 선행: phase2-feedback-problems.md (Session 1), phase1-2ndorder-problems.md

## 도구 정리

**표준 구조** — controller는 forward path, sensor는 feedback path:

```
 R ──▶(+)──▶ E ──▶[ C(s) ]──▶ u ──▶[ G(s) ]──▶ Y ──●──▶
        ▲(−)       controller        plant         │
        └──────────────[ H(s) ]◀──────────────────┘
                        sensor
```

| | 위치 | 정체 | 내가 바꾸나 |
|---|---|---|---|
| C(s) | forward | controller | ✅ 설계의 전부 |
| G(s) | forward | plant | ❌ 주어짐 |
| H(s) | feedback | sensor | ❌ (unity면 1) |

**PID controller** — 세 항이 병렬(= 합, B1(b)의 규칙):

```
C(s) = K_p + K_i/s + K_d·s
       ↑상수   ↑적분    ↑미분
u(t) = K_p·e(t) + K_i∫e dt + K_d·ė(t)
```

**특성방정식** `1 + C(s)G(s)H(s) = 0`. 이 근이 closed-loop pole이고, 시스템의 동역학을 전부 결정한다.
분자는 입력이 들어오는 방식만 정한다. 실전 계산법: `1 + CG = 0`의 양변에 분모를 곱해 다항식으로 정리.

**세 항이 특성방정식의 어디에 들어가나** (이 세션의 뼈대):

```
K_p  →  상수항        : 미는 세기, ωₙ
K_i  →  차수를 하나 올림, C(0)=∞ : steady-state error 제거
K_d  →  s의 계수      : 감쇠(ζ), pole을 왼쪽으로
```

**DC 관점 요약**: `e(∞) = R·1/(1+C(0)G(0))`. `C(0)` 또는 `G(0)`이 무한대면 오차가 정확히 0.
적분기(`1/s`)가 loop 안에 **하나라도** 있으면 step 오차는 0 — plant에 있든 controller에 있든.
미분항은 `C(0) = K_p`라 정상상태에 영향이 없다(감쇠를 공짜로 얻음).

> 표기: ✍️ = 네가 쓴 답 그대로 / 💬 = Claude가 덧붙인 해설·교정

---

## ✅ 워밍업 (2026-07-25, 14일 공백) — 전부 완료

### ✅ W1. closed-loop 재유도 — 자력, 지난번 미끄러진 지점 통과

- ✍️ `Y/R = G/(1+GH)` 즉답 → 💬 결과 말고 유도를 요구 → ✍️ `R − YH = E`, `EG = Y`
- 💬 B2에서 `R − H = E`로 틀렸던 **신호/블록 혼동이 교정된 채로 통과**

### ✅ W2. Pole은 어디로 가나 — 시간 도메인 유도로 "왜"까지 도달

- ✍️ pole = −(2+K) 자력, τ = 1/(2+K) → K=8이면 0.1 (원래 0.5에서 5배)
- ✍️ (b) 첫 답 "에러가 증폭됨" — 방향은 맞으나 "왜 **빨라지는지**"는 미해결
  (증폭되면 세게 밀 뿐, 정착이 빨라진다는 보장 없음 — 실제로 W5에서 반례)
- 💬 시간 도메인으로 유도 유도 → ✍️ `ẏ = −2y + K(R−y)`에서 y의 계수 **−(2+K)** 자력 도출.
  s-도메인 pole과 같은 숫자임을 확인 (Phase 0 ODE ↔ Phase 1 pole, 같은 것의 두 표기)
- 💬 정리: feedback = "목표에서 벗어난 거리에 비례하는 복원력" 추가 → plant 자신의 감쇠율에 그대로 더해짐.
  밀어야 할 양이 남은 거리에 비례해 자동으로 줄어들기 때문에 지수 감쇠가 빨라진다

### ✅ W3. 정착값 두 개 — 적분기의 발견

- 💬 질문: "K는 어느 도메인 상수야?" → 순수 gain은 도메인 불변(`L{K·e} = K·E`), P만 그렇고 I·D는 s가 붙어 dynamics 발생
- 💬 질문: "unity feedback이 뭐야?" → H=1, `E = R − Y`가 진짜 오차
- ⚠️ DC gain 방법 소환에 도움 필요 — **Session 1 B4에 이어 두 번째** (세션 감시 대상)
- 💬 final value theorem을 pole 관점으로 재유도: 정착값 c는 `Y(s)`의 **원점 pole의 residue**,
  s를 곱하는 건 그 pole을 지워 residue만 꺼내는 동작. ⚠️ 수렴할 때만 유효(sine에 쓰면 거짓말)
- ✍️ (a) y(∞) = 0.8, e(∞) = 0.2 자력
- ✍️ (b) `T = K/(s²+2s+K)` 자력, `T(0) = 1` → **e(∞) = 0, K와 무관** 자력
- ✍️ `G(0) = ∞` 자력 → `E/R = 1/(1+KG)`에서 오차가 0일 수밖에 없음
- ⚠️ "그 `1/s`가 시간 도메인에서 뭐냐"에 ✍️ "unit step function" — **신호로서의 1/s와 블록으로서의 1/s 혼동**.
  💬 블록이면 적분기(`L{∫y} = Y/s`). **B2의 신호/블록 혼동과 같은 종류, 두 번째 → 감시 대상**
- 💬 landing: 이 plant는 적분기를 공짜로 갖고 있다(`1/(s+2)`=속도, `1/s`=위치).
  B4(c)에서 예측한 "적분항"이 controller가 아니라 plant 쪽에 이미 있던 경우

### ✅ W4. 2차 표준형 매칭 — ζ 슬립 1회, 미니테스트로 자가 교정

- 💬 질문: "controller는 H를 말하는거야?" → forward/feedback path 역할표로 정리.
  `T = CG/(1+CGH)`의 분자에 H가 없는 비대칭이 `T→1/H`의 이유
- ✍️ ωₙ = √K 자력. ζ는 첫 답 "1" — **`2ζωₙ = 2`에서 ωₙ을 흘림** (IIR I1의 "상수 계수 누락" 패턴 재발)
  → 💬 미니테스트(K=4면 2ζωₙ=4≠2) → ✍️ **ζ = 1/ωₙ = 1/√K** 자가 교정
- ✍️ (b) (1,1), (0.5,2), (0.1,10) 전부 자력

### ✅ W5. 예측 — 이번 워밍업의 결론을 자력 발견 ★

- 💬 "ζ랑 ω가 뭐였지" → Phase 1 Session 3 재장착: `s = −ζωₙ ± ωₙ√(ζ²−1)`,
  기하학적으로 **원점거리 = ωₙ, 실수부 = −ζωₙ(감쇠), 허수부 = ωd(진동), cos θ = ζ**
- ✍️ **"실수부 다 −1이네"** — 핵심을 자력 발견. pole이 `Re = −1` 수직선에 갇혀 위아래로만 이동
- ✍️ (b) "아니, 진동이 생겨" — W2의 결론이 깨짐을 정확히 포착
- 💬 정량화: K=1과 K=100 모두 정착 ~4초로 **동일**, overshoot만 0% → 73%. **순손실**
- 💬 대수적 이유: `s² + 2s + K`에서 **s의 계수 2에 K의 손이 안 닿음**(근의 합 = −2 고정).
  W2의 1차 plant는 `s + (2+K)`라 K가 그 자리에 직접 들어갔던 것 — 이 차이가 전부
- ✍️ `C = K_p + K_d·s` 대입하여 **`s² + (2+K_d)s + K_p = 0` 자력 유도** →
  `ωₙ = √K_p`, `ζ = (2+K_d)/(2√K_p)`로 **두 손잡이가 독립**. pole placement의 원리 (Phase 5 예고)
- 💬 파생 질문 2개:
  - "1차 plant `1/(s+2)`에도 D 쓸 수 있어?" → 수학적으로 되지만 `pole = −(2+K_p)/(1+K_d)`라
    **K_d가 분모에 앉아 느려진다**. 1차는 pole 하나라 진동 자체가 불가 → 감쇠할 대상이 없음.
    D는 관성이 있는 시스템(pole ≥ 2)에 쓴다
  - "적분항이 두 개 된 거 아니야?" → 아님, `K_d·s`는 미분기. 적분기는 plant의 `1/s` 하나뿐(차수 그대로 2차).
    보너스: `C(0) = K_p`라 **D를 붙여도 e(∞)=0이 안 깨짐** — 감쇠를 공짜로 얻음

**워밍업 결론**: P는 pole을 밀 뿐이고 **어느 방향으로 밀지는 plant가 정한다**. D는 방향을 내가 정하게 해준다.

---

## 문제

이 세션의 plant는 **적분기가 없는** 쪽으로 돌아간다 — Session 1 B3/B4의 그것:

```
G(s) = 1/(s+2)        (τ=0.5인 1차 시스템. P만 쓰면 e(∞) = 2/(K+2) ≠ 0)
```

워밍업의 `1/(s(s+2))`는 적분기를 공짜로 갖고 있어서 I가 필요 없었다. 여기선 직접 붙여야 한다.

### P1. I항을 붙인다 — PI control ★

`C(s) = K_p + K_i/s`, unity feedback.

(a) 특성방정식을 세워 다항식으로 정리하라. (`1 + CG = 0`에 양변 곱해 정리)

(b) 차수가 몇 차가 되었나? P만 쓸 때는 1차였다. **무엇이 차수를 올렸나?**

(c) `e(∞)`를 구하라. `C(0)`을 보면 계산 없이도 알 수 있다 — 왜인가?

(d) 표준형 매칭으로 ζ와 ωₙ을 `K_p`, `K_i`의 식으로 써라.

### P2. 공짜가 아니다 — I의 대가

P1(d)의 결과를 보고 답하라. (계산보다 해석 문제)

(a) `K_p`를 고정하고 `K_i`만 키우면 ζ는 어떻게 되나? 응답 모양은?

(b) 오차를 빨리 없애려고 `K_i`를 키우는 것과, 진동을 피하려는 것이 **왜 서로 충돌**하는지
한 문장으로. (I항이 하는 일을 시간 도메인 언어로 생각해보라: 과거 오차의 누적이 지금의 u를 정한다면,
y가 목표에 **막 도달한 순간** 적분값은 어떤 상태인가?)

(c) 📌 예측: 그럼 이 충돌을 완화하려면 뭘 더 붙여야 하나? (한 단어)

### P3. Pole placement — 설계를 역방향으로 ★

이제 진짜 설계. 목표를 먼저 찍고 게인을 역산한다.

Plant `G(s) = 1/(s+2)`, controller `C(s) = K_p + K_i/s`.
사양: **ζ = 0.7, ωₙ = 10** (overshoot 약 5%, 정착 ~0.6초)

(a) `K_p`, `K_i`를 각각 구하라.

(b) 이 방법이 항상 통하는가? — 미지수는 2개(`K_p`, `K_i`), 맞춰야 할 계수도 2개(s의 계수, 상수항)라
정확히 풀렸다. Plant가 2차(`1/((s+2)(s+3))`)라면 특성방정식은 3차가 되고 맞출 계수는 3개인데
PI의 손잡이는 여전히 2개다. 무엇을 하나 더 붙이면 되나?

(c) 그래서 **PID가 세 항인 이유**를 한 줄로 정리하라.

### P4. D항의 어두운 면 — Phase 3 회수 ★

`K_d·s`를 그대로 구현하면 실전에서 터진다. 이유를 Phase 3의 지식으로 설명하라.

(a) 미분기 `D(s) = s`의 주파수 응답 크기 `|D(jω)|`는? ω가 커지면 어떻게 되나?
이건 어떤 종류의 필터인가?

(b) Phase 3 Session 2 F2에서 차분 필터 `y[n] = x[n] − x[n−1]`이 고역통과임을 확인했다.
디지털에서 미분은 차분이다. 센서 노이즈가 고주파에 몰려 있다면, D항은 무슨 짓을 하나?

(c) 드론 프로젝트에서 얻은 결론 두 개 — "필터는 지연을 만든다", "지연은 제어에 독이다" —
와 (b)를 합치면 딜레마가 하나 생긴다. 무엇인가?

(d) 실무 해법은 미분기에 저역통과를 물린 **filtered derivative**:
`K_d·s/(τ_f·s + 1)`. `τ_f`가 아주 작을 때와 아주 클 때 각각 무엇에 가까워지는지 확인하고,
`τ_f`가 무엇과 무엇 사이의 trade-off인지 한 줄로 써라.

### ✅ P5. 예측 → 실험 — **튜터의 예측이 빗나가고 사용자 직감이 맞음** ★

Plant `G(s) = 1/(s+2)`, step 입력. 네 경우 비교.

- ✍️ 예측 단계에서 **"1차 시스템엔 굳이 PID 다 할 필요 없는 거 아니야?"** 자발적 제기 →
  💬 근거 요구 → ✍️ 특성방정식 `(1+K_d)s² + (2+K_p)s + K_i = 0` 자력 유도,
  `K_d`가 **최고차항**에 앉는다는 것 발견
- ✍️ ζ 유도에서 `2ζωₙ`의 2 누락 (오늘 세 번째, 미니테스트 후 자가 교정) →
  `ζ = (2+K_p)/(2√(K_i(1+K_d)))`, `ωₙ = √(K_i/(1+K_d))`
- ✍️ 숫자: K_d=0 → ζ=0.424, K_d=1 → ζ=0.300 / 실수부 **−6 → −3** 자력
- 💬 landing: 같은 D항이 plant에 따라 정반대. 2차 plant는 `s³+(5+K_d)s²+...`로 감쇠 자리,
  1차 plant는 `(1+K_d)s²`로 최고차 자리 → ζ·ωₙ 둘 다 깎임. **D는 plant가 2차 이상일 때만**

**실측 (실행 결과)**:

| | 튜터 예측 | 실측 | |
|---|---|---|---|
| P (K_p=10) e(∞) | 0.167 | 0.1667 ✓ | |
| PI(50) overshoot | ~1% | 9.5% | zero |
| PI(200) overshoot | 23% | **30.8%** | zero가 키움 |
| PID overshoot | 37% | **20.3%** | derivative kick |
| PID 정착 | 2배 느림 | **1.030s vs 0.548s** ✓ | 실수부 −3 vs −6 |
| P3 설계 pole | ζ=0.7, ωₙ=10 | **−7±7.14j 정확 일치** ✓ | |
| P3 overshoot | 5% | **15.6%** | zero at −8.3 |

- ✍️ **"PI보다 PID가 overshoot 적은 거 아니야?"** — 실측이 사용자 손을 들어줌 (20.3% < 30.8%)
- 💬 **튜터 오류 인정**: `ζ → overshoot` 표는 **분자가 상수인 표준형 전용**.
  PI/PID는 항상 zero를 만든다 (`PI: zero at −K_i/K_p`). pole만 보고 모양을 판정한 것이 실수
- 💬 PI의 zero가 overshoot을 키우는 이유: `T = K_i·[기본응답] + K_p·s·[기본응답]` —
  **응답에 자기 미분을 섞는 것**이라 상승 구간에서 봉우리가 밀림
- 💬 PID가 낮은 진짜 이유 = **derivative kick**. `K_d`가 있으면 biproper →
  **`y(0+) = T(∞) = K_d/(1+K_d) = 0.5`**. final value theorem의 쌍둥이(`y(∞)=T(0)`, `y(0+)=T(∞)`).
  감쇠가 좋아서가 아니라 **출발선이 0.5라 올라갈 거리가 절반**이라서 — 좋은 이유가 아님
- 💬 실무 해법: **derivative on measurement** (미분을 `e`가 아니라 `−y`에만 걸어 setpoint 점프를 미분기가 못 보게)
- 💬 Fig 3 (P4 정량): 출력의 1% 노이즈가 `u`를 **±92**까지 흔듦(정상상태 필요 `u`=2인 시스템에서).
  `τ_f=10ms` 하나로 표준편차 28.37 → 1.47 (20배)

**P5의 교훈**: **pole만으로 응답 모양을 예측하면 안 된다.** ζ→overshoot 표는 zero 없는 시스템 전용이고
PI/PID는 항상 zero를 만든다. pole placement로 설계한 뒤 **반드시 시뮬레이션으로 확인**해야 하는 이유.

## Python 검증 (다 푼 뒤)

`phase2_pid_check.py` 실행:
- Fig 1 — P5의 네 경우 step response 비교
- Fig 2 — P3의 설계 검증: ζ=0.7, ωₙ=10 사양이 실제로 나왔는지 (overshoot ~5%, 정착 ~0.6s)
- Fig 3 — P4: 측정 노이즈를 섞었을 때 순수 D vs filtered D의 제어 입력 `u(t)` 비교
