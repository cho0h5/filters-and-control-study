# Phase 2 프로젝트 — 모터 위치 제어기 설계 (Session 4)

> 목표: Session 1~3에서 배운 도구를 **하나의 설계 흐름**으로 꿰어, 사양에서 시작해
> 게인·필터·loop rate를 전부 계산으로 결정한다. 시행착오 튜닝 없이.
> **Part A(시뮬레이션)는 Part B(Dynamixel 실물)의 예행연습이다** — 같은 plant 구조로 설계한 뒤
> 실물에서 어긋나는 부분이 곧 "모델의 어디가 틀렸는가"를 가리킨다.
> 선행: Session 1(feedback), Session 2(PID·pole placement), Session 3(Bode·margin·root locus)

## 규칙

- **구현·해석·결론은 전부 네가 한다.** Claude는 스펙·리뷰·막힐 때 힌트만.
- 디버깅은 **"답을 아는 미니 테스트"** 로 (Phase 3 프로젝트에서 my_iir 버그 4개를 이 방법으로 잡았다).
- 각 단계에서 **손으로 먼저 계산하고 코드로 검증**한다. 순서를 바꾸면 배우는 게 없다.
- 예측이 빗나가면 그게 가장 좋은 재료다. **왜 빗나갔는지**를 반드시 적는다.

---

# Part A — 시뮬레이션

## 설계 대상

**설계용 모델** (네가 알고 있다고 가정하는 것):

```
G(s) = 100 / (s(s+10))
```

- `1/s` = 위치는 속도의 적분 (워밍업 W3에서 확인한 그 구조)
- `1/(s+10)` = 모터 기계 시정수 τ_m = 0.1s
- 100 = 전체 이득 (전압 → 위치)

**실제 plant** (`plant_true()`로만 접근 가능, 설계에는 쓰지 말 것):

```
G_true(s) = 20000 / (s(s+10)(s+200))
```

전기 pole이 `s = −200`에 숨어 있다. 설계 단계에서는 **모르는 것으로 취급**하고,
M6에서 드러낸다. (실무에서 늘 벌어지는 일 — 모델에 없는 pole)

## 사양

| 항목 | 값 |
|---|---|
| step 응답 overshoot | ≤ 10% |
| 2% 정착 시간 | ≤ 0.5 s |
| 일정 외란(마찰 토크) 하 정상상태 오차 | 0 |
| phase margin | ≥ 45° |
| gain margin | ≥ 6 dB |
| 센서 노이즈 | 위치 측정에 σ = 0.001 rad (백색) |

---

## ✅ M1. 개루프 특성 파악

- ✍️ (a) pole = 0, −10 두 개. 적분기 있음. step에 대한 `e(∞) = 0` — 전부 자력
- ✍️ (b) 저주파 −20 dB/dec, 고주파 −40 dB/dec, corner ω=10. 위상 −90° → −180° 자력
  (부품별 극단 대입법과 `∠(1/z) = −∠z`는 재확인 필요했음 — Session 3 D2·D3와 같은 자리)
- 💬 (c) "그건 어떻게 알아?" → 세 가지 방법 정리: **① pole로 읽기**(`1/s²` → ramp),
  **② FVT로 확인**(`s·Y(s) → ∞` = 수렴 안 함, W3에서 단 조건이 여기서 작동),
  **③ 물리**(전압 일정 → 속도 일정 → 위치는 영원히 증가)
- 💬 **M1 결론: 적분기가 있는 plant는 open-loop 위치 제어가 원리적으로 불가능하다.**
  목표 위치에 대응하는 전압이라는 게 존재하지 않는다. B5의 open-loop compensation은
  `G(0)`이 유한해서 가능했던 것 — 여기선 `G(0)=∞`라 역보정할 대상이 없다. **feedback이 필수인 경우.**

## ✅ M2. Controller 구조 결정 ★ — 이 프로젝트의 개념적 핵심

- 💬 "D가 뭐야?" → disturbance. **`u`와 같은 지점(plant 입력단)에 더해지는 미지의 입력**.
  마찰 토크, 수평 팔의 중력, 바람. `R`(따라가야 할 것)과 `D`(무시해야 할 것)가 feedback의 두 임무
- ✍️ `Y/D = G/(1+CG)` 자력 유도
- 💬 "왜 0을 넣는 게 step 외란에 대한 거야?" → step의 `1/s`가 FVT의 `s`와 약분되어
  **항상 "전달함수에 s=0 대입"**. 바뀐 건 어느 전달함수를 보느냐뿐:
  `T(0)`은 setpoint 추종, `(Y/D)(0)`은 외란 억제
- ✍️ `1/G(0) = 0`, `(Y/D)(0) = 1/K_p` — **0이 아니다**

**M2(b)의 발견**: plant에 적분기가 있는데도 외란 오차가 남는다. 이유는 정상상태에 필요한 `u`가 다르기 때문:

```
외란 없음:  알짜 입력 = u = 0      →  u = K_p·e 에서 e=0으로 충족  ✅
외란 d   :  알짜 = u + d = 0
            u = −d 를 계속 공급해야 함  →  e = −d/K_p ≠ 0        ❌
```

Session 1 B4의 "오차가 없으면 미는 힘도 없다"가 **외란 쪽에서 재발**한 것.
**Dynamixel 수평 팔의 처짐 = d/K_p**가 정확히 이것.
일반 규칙: **적분기는 외란 진입점보다 앞쪽(controller 쪽)에 있어야 그 외란을 잡는다.**

- ⚠️ (c) "두 개만 필요" / "모터에 두 개, 외란에 하나?" → 💬 셈법 교정: P3의 논법은
  **맞춰야 할 계수 개수 vs 손잡이 개수** → ✍️ `s²` 계수가 `10 + 100K_d`가 됨을 자력 확인

```
PI만 :  s³ + 10s²          + 100K_p·s + 100K_i    손잡이 2, 계수 3  ❌
PID  :  s³ + (10+100K_d)s² + 100K_p·s + 100K_i    손잡이 3, 계수 3  ✅
```

**결론 `C = K_p + K_i/s + K_d·s`** — 세 항이 각각 다른 이유로 필요:
P는 `s` 계수, I는 **외란** 오차(setpoint만 봤으면 불필요했다), D는 `s²` 계수를 열어 3차 배치 완성.
Session 2 P3의 "2차 plant + I = 3차 ↔ PID 손잡이 3개"가 실물로 나타난 사례.

## M2. Controller 구조 결정 ★

세 항 중 무엇을 쓸지 **계산으로** 정한다. 암기로 "PID 다 넣자"가 아니다.

- (a) `G`에 이미 적분기가 있다. **step 목표에 대해 I항이 필요한가?** (워밍업 W3의 논리)
- (b) 그런데 사양에는 "**일정 외란 하** 정상상태 오차 0"이 있다.
  외란 `d`가 plant 입력단에 더해질 때, 오차 전달함수 `E/D`를 유도하고 `s→0`을 취해보라.
  I항 없이 이 사양을 만족할 수 있는가? **(a)와 답이 다른 이유는 무엇인가?)**
- (c) D항은 필요한가? Session 2에서 얻은 판정 기준(plant 차수)으로 답하라.
- (d) 결론: `C(s) =` ?

> 💡 (b)가 이 프로젝트에서 가장 중요한 개념 문제다. **같은 시스템이 setpoint에 대해서는
> 오차 0인데 disturbance에 대해서는 아닐 수 있다.** Dynamixel 수평 팔 실험이 바로 이것.

## ✅ M3. Pole placement로 게인 결정 ★ — 설계 실패 → 진단 → 구조 변경으로 해결

- 💬 "3차 기본형이 뭐야?" → 3차에는 표준형이 없다. **지배 2차 쌍 + 멀리 있는 실근**으로 설계:
  `(s² + 2ζωₙs + ωₙ²)(s + p)`, `p`를 `ζωₙ`의 5~10배 왼쪽에 두면 dominant pole approximation이 성립
- 💬 "인수분해해야 해?" → 반대. **이미 인수분해된 형태에서 출발해 전개**하고 계수를 맞춘다
- ✍️ ζ=0.6, ωₙ=13.3, p=40 선택 (💬 두 사양 모두 경계선 — 여유 0. 이 선택의 대가가 (d)에서 드러남)
- ✍️ 목표 계수 55.96 / 815.29 / 7075.6 자력 → **K_p=8.15, K_i=70.76, K_d=0.46** 자력

**(d) 검증 — pole은 완벽, 사양은 FAIL**:

```
목표 pole : −40, −7.98 ± 10.64j
실제 pole : −40, −7.98 ± 10.64j     ← 소수점까지 일치
실측      : overshoot 14.9% (사양 10%),  정착 0.248s     FAIL
```

- ✍️ "뭔가 숨겨진 게 있는 거야?" → 💬 숨은 건 없고 **네가 만든 zero**다.
  `C = (K_d s² + K_p s + K_i)/s`의 분자가 그대로 `T`의 zero가 됨 (−8.87 ± 8.68j).
  **Session 2 P5에서 튜터가 틀렸던 그 함정에 이번엔 설계가 걸린 것**
- ✍️ 해법 3안 중 **2번(derivative on measurement)** 선택 → ✍️ `Y/R` 자력 유도,
  **분모 = 1 + C·G로 원래와 동일**(pole 불변)이고 분자에서 `K_d·s²`만 사라짐을 확인
- ✍️ **"실제로는 Y를 못 얻으니 Y에 D를 못 걸잖아"** → 💬 반대다. `y`가 측정값이고 `e = r − y`는
  그 뺄셈. `r` 상수 구간에서는 `ė = −ẏ`로 두 방식이 동일하고, 차이는 `r` 점프 순간뿐(= derivative kick).
  진짜 문제는 `y`를 못 얻는 게 아니라 **깨끗한 `ẏ`를 못 얻는 것**(M5)
- ✍️ **"Dynamixel의 Present Velocity가 내부적으로 어떻게 계산되는지는 모르는 거 아니야?"** —
  💬 정확한 의심. 별도 센서가 아니라 내부 미분+필터일 가능성이 높고 **그 `τ_f`와 지연을 모른다**
  = 모델에 없는 필터가 loop 안에 앉아 위상을 먹는 D5의 상황. → **Part B 실험 ②로 추가**

**결과가 예상과 반대** — D on measurement가 overshoot을 **더 나쁘게**(33.6%) 만듦:

- ✍️ 원인 자력 진단: zero **개수**가 아니라 **위치**. `|z|`가 12.4 → 8.68로 **원점에 더 가까워짐**
  (`ζωₙ = 7.98`의 1.55배 → 1.09배). 경험 규칙 `|z|/(ζωₙ) > 4~5`면 무시 가능 — 둘 다 한참 미달
- 💬 튜터 정정: **derivative on measurement의 확실한 효과는 `u`의 derivative kick 제거이지
  overshoot 감소가 아니다.** 게인 조합에 따라 어느 쪽으로도 갈 수 있음

- ✍️ **"근데 zero가 그럼 나쁜 거야?"** → 💬 아니다. **같은 zero가 두 군데에서 정반대 역할**:

```
L(s)의 zero  →  위상을 +90°씩 벌어옴  →  안정성 ↑   ✅
T(s)의 zero  →  응답에 미분 섞음      →  overshoot ↑ ❌
```

  표준 PID에서는 이 둘이 같은 zero라 거래가 강제된다. **setpoint weighting이 하는 일이 정확히 이 분리** —
  `L`에는 zero를 남기고 `T`에서만 없앤다. (RHP zero = non-minimum phase는 진짜로 나쁨:
  초기 역방향 응답 + 대역폭에 원리적 상한. 자전거 핸들, 드론의 전진 시 고도 강하)

**최종 해법 — I-PD 구조** (`P`와 `D` 모두 측정값에만, `I`만 오차에):

```
PID   (D on error)      OS 14.9%   ts 0.248s   zero −8.87±8.68j   FAIL
PI-D  (D on meas.)      OS 33.6%   ts 0.562s   zero −8.68         FAIL
I-PD  (P,D on meas.)    OS  8.8%   ts 0.474s   zero 없음           PASS ✅
```

- zero가 없어지니 **ζ 표가 정확히 작동**(ζ=0.6 예측 9.5% vs 실측 8.8%)
- **외란 응답은 세 구조가 완전히 동일**(최대 편차 0.0882 rad, `y(∞)→0`) —
  `Y/D = G/(1+CG)`가 setpoint weighting과 무관하므로. M2에서 I를 넣은 이유가 그대로 살아 있음
- 잃은 것은 초기 추진력뿐(정착 0.248 → 0.474s)

**M3의 교훈**: **pole placement는 pole만 놓는다.** 응답 모양의 나머지 절반은 분자가 정하고,
분자는 controller **구조**가 정한다.

## ✅ M4. 마진 확인 — 사양 전부 통과, 그리고 type-2의 정체

```
ω_gc = 45.07 rad/s   PM = 79.4°   (사양 ≥45 ✅)
−180° 교차(ω>0) 없음  →  GM = ∞    (사양 ≥6dB ✅)
vector margin = 1.000              (권장 ≥0.5)
위상: ω→0에서 −179.99°  →  ω→∞에서 −90.04°
```

- ✍️ 분모 `s²` = **적분기 2개**(plant의 1/s + controller의 I) 자력 → DC에서 이미 −180°
- 💬 그럼 PM 79.4°는 어디서? **zero 두 개**. `ω_gc`에서 위상 장부가 정확히 맞는다:

```
1/s²      : −180.0°
1/(s+10)  :  −77.5°
zero 2개  : +156.9°
──────────────────
합계      : −100.6°   →  PM = 79.4°  ✓
```

- 💬 **zero가 없으면 이 시스템은 어떤 게인으로도 불안정**하다. I만 쓰면 특성방정식이
  `s³ + 10s² + 100K_i`로 `s` 계수가 0이라 Routh 조건을 절대 만족 못 함
- 💬 **구조 요약**: 적분기 2개가 −180°의 빚을 깔고 시작하고, zero 2개가 +180°를 벌어 갚는다.
  **`K_p`와 `K_d`는 성능 손잡이가 아니라 안정성 자체를 사는 대가.**
  Session 2의 "D가 감쇠를 준다"(대수적: `s²` 계수)를 주파수 영역으로 옮기면
  **"zero를 심어 위상 +90°를 벌어온다"** = 고전 제어의 **lead compensator**
- 💬 일반화: **적분기를 하나 넣을 때마다 위상 −90°의 빚을 진다.** M2에서 외란 때문에
  I를 넣기로 한 결정의 청구서가 여기 있었다

검증 코드: `phase2_project_check.py` (M3/M4, 실행 대행분). **M5부터는 사용자 구현.**

## M5. 노이즈와 필터 — 위상 예산 쓰기 ★★

센서 노이즈 σ=0.001 rad을 넣는다.

- (a) 순수 미분 `K_d·s`로 시뮬레이션하고 **제어 입력 `u(t)`** 를 그려라.
  (출력 `y`만 보면 안 된다 — Session 2 Fig 3의 교훈)
  `u`의 표준편차와 최댓값은? 정상상태에 필요한 `u`의 몇 배인가?
- (b) filtered derivative `K_d·s/(τ_f s+1)`로 바꾼다. **`τ_f`를 계산으로 결정하라**:
  - 위상 예산에서 몇 도를 쓸 수 있는가? (M4의 PM에서 45°를 남기고)
  - `ω_gc`에서 1차 저역통과가 만드는 위상 지연은 `−atan(ω_gc·τ_f)`
  - 그 예산에 맞는 `τ_f`의 상한은?
- (c) 그 `τ_f`로 노이즈가 얼마나 줄었는지 (a)와 같은 방식으로 측정하라.
  **위상 몇 도를 주고 노이즈 몇 배를 샀는지** 한 줄로 정리.

## M6. 숨은 pole — 모델이 틀렸을 때 ★★

이제 `plant_true()`(전기 pole 포함)로 바꿔서 같은 controller를 돌린다.

- (a) 설계용 모델과 실제 plant의 Bode를 겹쳐 그려라. **어디서부터 갈라지는가?**
  그 주파수와 `ω_gc`의 관계는? 왜 지금까지 무시해도 괜찮았는가?
- (b) 실제 plant 기준으로 PM/GM을 다시 계산하라. 얼마나 나빠졌는가?
- (c) **게인을 올려가며 발진 지점을 찾아라.** 설계용 모델로는 몇 배까지 안전하다고 예측되고,
  실제로는 몇 배에서 발진하는가? root locus로도 확인.
- (d) 결론: "무시해도 되는 pole"이라는 판단은 무엇에 의존하는가?

## M7. 이산화 — 진짜 구현으로 ★★

지금까지는 연속시간이었다. 실제 코드는 이산이다. **Phase 3가 여기서 회수된다.**

- (a) 설계한 `C(s)`를 bilinear transform으로 이산화하라 (Phase 3 Session 3의 그 방법).
  `my_iir`을 재사용해도 좋고 직접 짜도 좋다.
- (b) 계산 지연을 모델링한다: 측정 시점과 출력 시점 사이에 **1 샘플 지연**이 있다.
  loop rate `f_s`일 때 이 지연이 `ω_gc`에서 먹는 위상은 몇 도인가?
- (c) **최소 loop rate를 계산하라**: PM 45°를 유지하려면 `f_s`가 얼마 이상이어야 하는가?
  (M5에서 필터에 쓴 예산도 함께 고려할 것 — 예산은 하나뿐이다)
- (d) 계산한 `f_s`와 그 절반, 두 배로 각각 이산 시뮬레이션을 돌려 예측을 검증하라.
- (e) 📌 **최종 결론**: 사양에서 시작해 게인·`τ_f`·`f_s`가 어떻게 정해졌는지 한 페이지로 정리.
  **각 숫자가 어떤 제약에서 나왔는지**를 명시할 것.

---

# Part B — Dynamixel 실물 (Part A 완료 후)

Part A의 M1~M7이 그대로 하드웨어 절차가 된다. 상세 스펙은 Part A 완료 후 작성.

| 실물 단계 | 대응하는 Part A |
|---|---|
| PWM 모드로 loop을 열고 step 응답 로깅 → `G·H` 동정 | M1 (단, 모델을 **실측으로** 얻는다) |
| 속도·위치 응답으로 차수와 τ 확인 | M1(a) |
| **Present Velocity의 정체 규명** (아래 ★) | M3 파생 질문 |
| 내장 PID 게인으로 P → PI → PID 재현 (수평 팔 = 일정 외란) | M2(b) |
| Part A의 게인을 실물에 적용 | M3 |
| D 게인을 올려 chattering 관측(귀로 들림) → filtered derivative | M5 |
| loop rate를 낮춰가며 발진 지점 → PM 역산 | M7(c)(d) |
| 백래시 히스테리시스 측정, 엔코더 스케일 검증 | (모델에 없는 것들) |

### ★ Present Velocity의 정체 규명 (M3 파생 질문에서 추가)

Dynamixel의 `Present Velocity`는 별도 속도 센서가 아니라 **내부에서 위치를 미분한 값**일 가능성이 높다.
그렇다면 어떤 필터가 들어 있고, 그 `τ_f`와 지연을 우리는 모른다 — **모델에 없는 필터가 loop 안에
앉아 위상을 먹는 상황**(Session 3 D5). 남의 `τ_f` 선택이 내 대역폭에 맞다는 보장이 없다.

```
① 노이즈 수준: 일정 속도로 회전시키고 Present Velocity의 표준편차 측정
                → 내가 Present Position을 직접 미분한 값과 비교.
                  훨씬 매끈하면 필터가 있다는 증거

② 지연:        step을 주고 Present Velocity와 (Present Position을 수치미분한 값)을
                동시에 로깅 → 두 곡선의 시간차가 곧 내부 필터의 지연
```

②가 핵심이다. **내가 만든 미분값과 서보가 준 미분값을 나란히 놓으면 그 차이가 남의 필터의 정체다.**
지연을 알면 위상 예산에서 몇 도를 떼일지 계산할 수 있고, 과하면 위치를 직접 미분해
**내 `τ_f`로 필터링하는 편이 낫다.**

**실물에서만 만나는 것들**: backlash(비선형), 비틀림 공진, actuator 포화와 windup,
통신 지연의 변동(jitter), 엔코더 양자화. 각각이 Part A의 어느 가정을 깨는지 기록할 것.

---

## 진행 기록

| 단계 | 상태 | 메모 |
|---|---|---|
| M1 | ✅ 2026-07-25 | 적분기 있는 plant는 open-loop 위치 제어 불가 |
| M2 | ✅ 2026-07-25 | **setpoint 오차 0 ≠ 외란 오차 0**. PID 세 항 전부 필요 |
| M3 | ✅ 2026-07-25 | pole 완벽 배치했으나 zero 때문에 FAIL → **I-PD 구조로 PASS** |
| M4 | ✅ 2026-07-25 | PM 79.4°, GM=∞, VM=1.0. type-2의 −180° 빚을 zero가 갚는 구조 |
| M5 | ⬜ **다음 시작 지점** | 노이즈 σ=0.001 → `u(t)` 관측 → `τ_f`를 위상 예산에서 계산 |
| M6 | ⬜ | 숨은 pole(−200) 드러내기, 발진 지점 찾기 |
| M7 | ⬜ | 이산화 + 계산 지연 → 최소 loop rate |
| Part B | ⬜ | Dynamixel 실물 |

**M5 재개에 필요한 값** (전부 확정됨):

```
설계 모델   G = 100/(s(s+10))          실제 plant  G_true = 20000/(s(s+10)(s+200))
게인        K_p = 8.15,  K_i = 70.76,  K_d = 0.46      구조: I-PD
pole        −40, −7.98 ± 10.64j        ω_gc = 45.07 rad/s,  PM = 79.4°
현재 성능   overshoot 8.8%,  정착 0.474s   (사양 10%, 0.5s)
```

M5(b)에서 쓸 예산: PM 79.4°에서 45°를 남기면 **34.4°**를 필터에 쓸 수 있다.
