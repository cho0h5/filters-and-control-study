# Phase 4 Session 1 — 동치 사슬 (rank · det · 선형독립 · 역행렬)

> 목표: `rank` ↔ `det ≠ 0` ↔ `열 선형독립` ↔ `역행렬 존재`가 **같은 하나의 사건**을 네 각도에서
> 부른 것임을 세운다. Phase 5(상태공간) 진입용 무장이지 선형대수 전체가 아니다.
> 진행 방식: **작은 계산 하나 → 즉시 확인**을 번갈아 (예측 질문을 앞에 쌓지 않는다)

## ✅ W0. 행렬 = 변환, 열 = 도착지

$$
A = \begin{bmatrix} 2 & 0 \\ 0 & 3 \end{bmatrix}
\qquad
\mathbf{e}_1 = \begin{bmatrix} 1 \\ 0 \end{bmatrix},\quad
\mathbf{e}_2 = \begin{bmatrix} 0 \\ 1 \end{bmatrix}
$$

- ✍️ "변환 후 기저벡터가 움직인다" 자력 — 행렬을 표가 아니라 **변환**으로 읽는 관점 확보
- 💬 정밀화: $A\mathbf{e}_1 = (2,0)$, $A\mathbf{e}_2 = (0,3)$ — 각각 $A$의 **첫째 열·둘째 열**

> **행렬의 열 = 기저벡터가 도착한 자리.** 행렬은 숫자 표가 아니라
> "$\mathbf{e}_1$은 여기로, $\mathbf{e}_2$는 저기로 보내라"는 **지시서**다.
> 이 세션의 나머지 전부가 여기서 나온다.

## ✅ L1~L2. 차원이 무너지는 경우

$$
B = \begin{bmatrix} 1 & 2 \\ 2 & 4 \end{bmatrix}
$$

- ✍️ $B\mathbf{e}_1 = (1,2)$, $B\mathbf{e}_2 = (2,4)$ — W0의 규칙을 즉시 적용(계산 불필요)
- ✍️ $(2,4) = 2 \cdot (1,2)$ 자력 → 두 도착점이 **같은 직선 위**

임의의 벡터가 어디로 가는지 전개하면:

$$
B\begin{bmatrix}x\\y\end{bmatrix}
= x\begin{bmatrix}1\\2\end{bmatrix} + y\begin{bmatrix}2\\4\end{bmatrix}
= x\begin{bmatrix}1\\2\end{bmatrix} + 2y\begin{bmatrix}1\\2\end{bmatrix}
= (x+2y)\begin{bmatrix}1\\2\end{bmatrix}
$$

$x, y$가 무엇이든 결과는 항상 $(1,2)$의 배수 — **평면 전체가 직선 하나로 뭉개진다.**

| | 하는 일 |
|---|---|
| $A$ | 평면 → 평면 (2배·3배로 늘어난 평면) |
| $B$ | 평면 → **직선** (2차원이 1차원으로) |

**L2 되돌리기**: ✍️ "못 알아내지" 자력. 실제로

$$
B(1,0) = (1,2), \qquad B(3,-1) = (1,2)
$$

**서로 다른 입력이 같은 출력** → 출력만 보고 입력을 되찾을 수 없다 = **역행렬이 없다**의 뜻.
정보가 날아간 것이다.

## ✅ 동치 사슬 — 네 이름, 한 사건

| 이름 | $B$에서 관찰한 것 |
|---|---|
| 열이 **선형종속** | $(2,4) = 2(1,2)$ — 둘째 열이 첫째의 배수 |
| $\det = 0$ | 평면이 직선으로 뭉개져 **넓이가 0** |
| $\text{rank} = 1$ | 도착한 곳의 차원이 1 |
| **역행렬 없음** | 서로 다른 입력이 같은 출력 |

$$
\text{열 선형독립} \iff \det \ne 0 \iff \text{rank} = n \iff \text{역행렬 존재}
$$

넷 중 하나가 깨지면 나머지 셋도 같이 깨진다. **"차원이 무너졌는가"** 하나를 네 각도에서 물은 것.

- 열 = 기저벡터의 도착지 → 겹치면 도착 공간이 납작해진다
- $\det$ = 넓이(부피) 배율 → 납작해지면 0
- $\text{rank}$ = 도착 공간의 차원 → 납작해진 만큼 줄어든다
- 역행렬 = 되돌리기 → 뭉개진 정보는 못 되돌린다

**검산** ($2\times2$에서 $\det M = ad - bc$): ✍️ $\det A = 6$, $\det B = 0$ 자력.
$A$는 단위정사각형(넓이 1)을 $2\times3$ 직사각형(넓이 6)으로 만든다 — 기하적 의미와 일치.

## ✅ L3. $\det$의 한계 — 그래서 rank가 필요하다

$$
C = \begin{bmatrix} 1 & 0 & 1 \\ 0 & 1 & 1 \\ 0 & 0 & 0 \end{bmatrix}
\qquad
D = \begin{bmatrix} 1 & 2 & 3 \\ 2 & 4 & 6 \\ 3 & 6 & 9 \end{bmatrix}
$$

둘 다 $\det = 0$. 열(= 도착지)을 보면:

- $C$의 열: $(1,0,0),\ (0,1,0),\ (1,1,0)$ — 셋째 = 첫째 + 둘째
- $D$의 열: $(1,2,3),\ (2,4,6),\ (3,6,9)$ — 전부 $(1,2,3)$의 배수 ($B$와 같은 패턴)

- ✍️ **"각각 2차원, 1차원"** 자력 정답

$$
C:\ 3\text{차원} \to \text{평면}, \quad
D:\ 3\text{차원} \to \text{직선}, \quad \text{그런데 } \det \text{는 둘 다 } 0
$$

**$\det$은 "부피가 0이냐"만 답한다.** 평면으로 뭉갰든 직선으로 뭉갰든 부피는 똑같이 0이라 구분 못 함.
$\text{rank}$는 그걸 센다:

$$
\text{rank} = \text{도착 공간의 차원} = \text{독립인 열의 개수}
$$

$$
\text{rank}\,A = 2\ (\text{꽉 참}), \quad \text{rank}\,B = 1, \quad
\text{rank}\,C = 2, \quad \text{rank}\,D = 1
$$

> **$\det$은 예/아니오, $\text{rank}$는 몇 차원인지.**

### 왜 Phase 5가 $\det$ 대신 $\text{rank}$를 쓰는가

가제어성 판정이 $\text{rank}$로 되어 있는 이유:

1. 입력이 여러 개면 가제어성 행렬이 **정사각형이 아니고**, 정사각형이 아니면 $\det$이 정의되지 않는다.
   $\text{rank}$는 어떤 모양이든 센다
2. "제어 못 하는 방향이 몇 개인지"를 알려면 0/1이 아니라 **숫자**가 필요하다
---

## 진행 기록

| 항목 | 상태 | 메모 |
|---|---|---|
| W0 행렬=변환, 열=도착지 | ✅ 2026-07-28 | "기저벡터가 움직인다" 자력, 열과의 대응은 정밀화 필요했음 |
| L1 차원 붕괴 | ✅ 2026-07-28 | $(2,4)=2(1,2)$ 자력 발견 |
| L2 역행렬의 뜻 | ✅ 2026-07-28 | "못 알아내지" — 두 입력이 같은 출력으로 자력 도달 |
| 동치 사슬 + $\det$ 검산 | ✅ 2026-07-28 | $\det A = 6$, $\det B = 0$ 자력 |
| L3 rank vs det | ✅ 2026-07-28 | 2차원/1차원 자력 — $\det$이 구분 못 하는 지점 |

**총평**: 전 문항 자력. Phase 2 M5에서 막혔던 것과 대비되는데, 차이는 내용 난이도가 아니라
**한 번에 한 계산씩 물었다**는 점. 3Blue1Brown *Essence of Linear Algebra* 1~7화가 이 세션과 정확히 겹친다.

**다음**: Session 2 (고유벡터와 대각화, phase4-eigen-problems.md)
