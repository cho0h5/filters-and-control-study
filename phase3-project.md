# Phase 3 — 프로젝트: 노이즈 낀 센서 신호 필터링

> 목표: Session 1~3에서 배운 걸 전부 코드로 회수한다.
> **설계는 scipy에 맡기되, 필터를 "돌리는" 루프는 직접 구현**해서 scipy 결과와 비트 단위로 비교.
> FIR(convolution)과 IIR(recursion)을 같은 신호에 적용해 트레이드오프(지연·날카로움·계산량)를 눈으로 확인한다.
> 규칙: 구현 코드는 전부 본인이 작성. Claude는 스펙·리뷰·막힐 때 힌트만.

## 시나리오

드론의 고도 센서. 실제 고도는 천천히 변하는데(저주파), 센서 출력엔 진동 노이즈(고주파)가 잔뜩 껴 있다.
저역통과 필터로 노이즈를 걷어내되, **지연이 얼마나 생기는지**도 함께 본다 — 제어 루프에 물릴 신호라면 지연이 곧 성능이니까 (Phase 2 예고편).

## 데이터 생성 (이것만 제공)

```python
import numpy as np

def make_signal(fs=100, dur=10.0, seed=0):
    """fs: 샘플링 주파수 Hz, 반환: (t, clean, noisy)"""
    rng = np.random.default_rng(seed)
    t = np.arange(0, dur, 1/fs)
    clean = (1.0*np.sin(2*np.pi*0.3*t)          # 천천히 오르내리는 고도 (0.3 Hz)
             + 0.5*np.sin(2*np.pi*0.8*t))        # 기동 성분 (0.8 Hz)
    noise = (0.4*np.sin(2*np.pi*23*t)            # 모터 진동 (23 Hz)
             + 0.3*rng.standard_normal(len(t)))  # 백색 잡음
    return t, clean, clean + noise
```

신호 대역 ≤ 0.8 Hz, 노이즈 ≥ 23 Hz + 백색 → 컷오프는 그 사이(예: 2 Hz)로.

## 마일스톤

### ✅ M1. FIR 설계 + 컨볼루션 직접 구현 (2026-07-11 완료)
- [x] `scipy.signal.firwin(numtaps=51, cutoff=2, fs=100)` 으로 계수 h 획득
- [x] **`my_convolve(x, h)`를 순수 루프로 직접 구현** — 이중 루프 구조를 세우는 과정에서 "x[n]·h[n]" element-wise 곱 시도 → "x랑 h가 시간축이야 주파수축이야?" 질문으로 이어짐 → 컨볼루션 정리(시간축 convolution ↔ 주파수축 곱)의 양변을 구분하며 해소. 수식↔코드 대응표로 자력 완성
- [x] `signal.lfilter(h, [1], x)` 와 비교: max diff 6.7e-16 < 1e-12 ✓. 보너스: impulse 테스트로 "δ 입력 → h 자신" 재현
- [x] h 좌우 대칭 확인 → 예상 지연 = 25샘플 (M3에서 측정으로 검증 예정)

### ✅ M2. IIR 설계 + 재귀 루프 직접 구현 (2026-07-11 완료)
- [x] `butter(2, 2, fs=100)` 획득 — 처음 `a, b = ...`로 **반환 순서 뒤집힌 버그**(리뷰에서 발견, 직접 수정). "재귀 루프가 뭐야" 질문 → 피보나치 recurrence와 같은 구조임을 확인 후 자력 구현
- [x] pole 체크: 0.911±0.081j 켤레쌍(I2c), |p|=0.915 < 1 안정 ✓. angle≈0.09 = z=1 근처 = 저주파를 살리는 배치. Σb=Σa → DC gain 1 정규화도 발견
- [x] `my_iir` 구현 — 디버깅 여정: ① 경계 체크 누락(음수 인덱스가 Python에선 wrap되는 조용한 버그) ② 계수-과거 짝 한 칸 어긋남(a[0]·y[n−1]) ③ 부호 +/− ④ i=0 포함으로 자기 자신 빼서 전부 0. **매 단계 "답을 아는 미니 테스트"(I1의 δ→0.1·0.9ⁿ)가 판정** — 네 버그 전부 이 테스트로 잡음
- [x] `lfilter` 대조: max diff 7.3e-15 < 1e-12 ✓. 계수 5개 (vs FIR 51개 — M3에서 성능 비교)

### ✅ M3. 비교 — Session 3에서 말로 한 트레이드오프를 숫자로 (2026-07-11 완료)
- [x] 플롯: clean / noisy / FIR 출력 / IIR 출력 (전체 10초 + 4~6초 확대)
- [x] 지연 측정: FIR 24샘플(이론 25, cross-correlation 해상도 ±1), IIR 12샘플(비선형 phase라 "벌크" 지연). 측정 원리(모든 shift에서 내적 → argmax)와 covariance와의 관계(centering·정규화 없는 cov를 shift별로 훑은 것)까지 파고듦
- [x] 계수 51개 vs 5개, 정렬 후 RMS 0.0611 vs 0.0606 (필터 전 0.4174) — 억제력 동률. 추가 실험(자발적 질문 "FIR을 25-tap으로 줄이면 똑같은 거 아니야?"): 지연 12로 IIR과 동률이 되지만 RMS 0.0707 — 예측(4배 악화)과 달리 1.16배만 악화, 노이즈가 어디 있느냐(23Hz는 깊은 차단영역)에 달린 문제임을 발견
- [x] 결론 한 단락 (본인 작성, 리뷰 2회 반영 — 수치 오류 0.7→0.0707 교정, 반대 논거 추가): 이 드론에 뭘 쓸 건가? 근거는? 비슷한 RMS에서는 빠르게 움직이는 드론엔 딜레이가 적은 iir이 유리. 등속도 운동한다고 가정하면 fir는 delay가 두배이기때문에 고도가 변하는 것도 두배 차이남. fir의 delay를 줄이기위해 window를 25로 줄이면 rms가 0.06에서 0.07로 증가. 이 경우에도 iir이 우세. 또한 드론 고도 측정에서 파형의 모양은 중요하지 않기 때문에 linear phase가 필요없음

### ✅ M4. (보너스) zero-phase 트릭 (2026-07-11 완료)
- [x] 실측: filtfilt 지연 1샘플(≈0), RMS 0.0597 — 전 필터 중 최고. 추론 둘 다 자력: "거꾸로 돌리면 지연이 반대방향"(+12−12=0, 위상 φ−φ 소거 = zero-phase), "미래 데이터가 필요해서 실시간 불가". 크기는 |H|² 두 번 적용됨도 확인. 결론: 실시간(제어)=lfilter, 오프라인(사후 분석)=filtfilt

## 완료 기준
- my_convolve, my_iir 둘 다 scipy와 1e-12 이내 일치
- M3 결론을 phase3-project.md 하단에 직접 작성
